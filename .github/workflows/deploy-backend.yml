name: Deploy Backend API

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging
          - prod
      run_migrations:
        description: 'Run database migrations'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'
  push:
    branches:
      - master
    paths:
      - 'api/**'
      - '.github/workflows/deploy-backend.yml'

permissions:
  id-token: write
  contents: read

env:
  NODE_VERSION: '20.x'
  AZURE_RESOURCE_GROUP: spacecityeidolons-rg
  AZURE_APP_SERVICE_NAME: spacecity-api-dev-xdtfmqqqnpcha

jobs:
  build:
    name: Build Backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: api/package-lock.json

      - name: Install dependencies
        working-directory: ./api
        run: npm ci

      - name: Generate Prisma Client
        working-directory: ./api
        run: npm run prisma:generate

      - name: Build
        working-directory: ./api
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-build
          path: |
            api/dist/
            api/node_modules/
            api/package.json
            api/package-lock.json
            api/prisma/

  deploy:
    name: Deploy to Azure App Service
    runs-on: ubuntu-latest
    needs: build
    environment: ${{ github.event.inputs.environment || 'dev' }}
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: backend-build
          path: .

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to App Service
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_APP_SERVICE_NAME }}
          package: ./api

      - name: Run database migrations
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.run_migrations == 'true'
        run: |
          # Set database URL from Key Vault
          DATABASE_URL=$(az keyvault secret show \
            --vault-name $(az keyvault list -g ${{ env.AZURE_RESOURCE_GROUP }} --query "[0].name" -o tsv) \
            --name database-url \
            --query value -o tsv)
          
          cd api
          export DATABASE_URL="$DATABASE_URL"
          npm run prisma:migrate:prod

      - name: Deployment Summary
        run: |
          echo "## Backend Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment**: ${{ github.event.inputs.environment || 'dev' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **App Service**: ${{ env.AZURE_APP_SERVICE_NAME }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Health Check**: https://${{ env.AZURE_APP_SERVICE_NAME }}.azurewebsites.net/health" >> $GITHUB_STEP_SUMMARY
