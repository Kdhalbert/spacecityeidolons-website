## Summary

Implements Test-Driven Development (TDD) for User Story 1: Public Landing Page & Invite Requests. This PR contains ONLY the tests (RED phase) - implementation will follow in subsequent PRs.

## TDD Approach - RED Phase ✅

Following constitution's TDD requirement, this PR writes comprehensive tests first. All tests currently **FAIL** as expected since no implementation exists yet.

## Test Coverage (T070-T074)

### T070: Schema Validation Unit Tests
**File:** `api/src/schemas/inviteRequest.schema.test.ts`
- ✅ Tests `createInviteRequestSchema` for email, name, platform validation
- ✅ Tests `updateInviteRequestSchema` for status updates and admin notes
- ✅ Tests `inviteRequestResponseSchema` for API responses
- ✅ Validates whitespace trimming, field length limits, enum values
- **11 test cases**

### T071: POST /api/invites Integration Test
**File:** `api/tests/integration/invites.test.ts`
- ✅ Creates Discord and Matrix invite requests
- ✅ Validates unauthenticated access (public endpoint)
- ✅ Tests duplicate email detection per platform
- ✅ Allows same email across different platforms
- ✅ Tests request validation and error responses
- **11 test cases**

### T072: GET /api/invites Integration Test
**File:** `api/tests/integration/invites.test.ts`
- ✅ Lists invite requests (admin-only)
- ✅ Tests pagination and filtering by status/platform
- ✅ Tests authentication and authorization
- ✅ Tests PATCH endpoint for admin status updates
- **11 test cases**

### T073: InviteRequestForm Component Test
**File:** `src/components/InviteRequestForm.test.tsx`
- ✅ Renders Discord and Matrix forms with proper messaging
- ✅ Tests form validation (email, name, message)
- ✅ Tests submission flow and success states
- ✅ Tests loading states and error handling
- ✅ Tests accessibility (labels, aria-attributes, keyboard nav)
- ✅ Tests duplicate email error display
- **28 test cases**

### T074: E2E Invite Request Flow Test
**File:** `e2e/invite-request.spec.ts`
- ✅ Complete user journey from landing to success
- ✅ Tests Discord and Matrix workflows
- ✅ Validates browser-level form validation
- ✅ Tests keyboard accessibility
- ✅ Tests error recovery and retry logic
- ✅ Validates landing page performance (SC-001, SC-016)
- **15 test cases**

## Total Test Count

**76 test cases** covering all acceptance scenarios for User Story 1

## Validation

All tests are confirmed to FAIL (RED state):
- ❌ Schema not implemented yet
- ❌ API routes not implemented yet
- ❌ InviteRequestForm component not implemented yet
- ❌ Landing page not updated yet

This is expected and correct for TDD RED phase.

## Next Steps

1. **Merge this PR** to establish test baseline
2. **Implement backend** (T075-T080):
   - Create InviteRequest Zod schema
   - Implement InviteRequestService
   - Create POST /api/invites endpoint
   - Create GET /api/invites endpoint (admin)
   - Add duplicate email validation
3. **Implement frontend** (T081-T092):
   - Refactor HomePage with community info
   - Create InviteRequestForm component
   - Add form validation
   - Integrate with API
4. **Watch tests turn GREEN** ✅

## Context

Follows constitution's TDD mandate: "Tests must be written before implementation." This establishes clear acceptance criteria and prevents scope creep during implementation.
