## Summary

Sets up comprehensive testing infrastructure for frontend development using Vitest for unit/component tests and Playwright for E2E tests.

## Changes

- **Dependencies** (+99 packages):
  - Vitest: Test runner with jsdom environment
  - @testing-library/react: Component testing utilities
  - @testing-library/jest-dom: DOM matchers
  - @testing-library/user-event: User interaction simulation
  - @playwright/test: E2E testing framework

- **Configuration**:
  - vitest.config.ts: Configure unit/component testing with globals, jsdom, coverage
  - playwright.config.ts: Configure E2E testing with chromium browser

- **Test Utilities**:
  - src/test/setup.ts: Testing Library matchers and cleanup
  - src/test/utils.tsx: Helper functions and mock data

- **Sample Tests** (verified working):
  - Button component: 6 unit tests (all passing)
  - Homepage E2E: 2 tests (all passing)

- **Test Scripts**:
  - npm test: Run unit/component tests
  - npm run test:ui: Interactive test interface
  - npm run test:coverage: Generate coverage report
  - npm run e2e: Run E2E tests
  - npm run e2e:ui: Interactive E2E test interface

## Verification

✅ All unit tests pass (6/6)
✅ All E2E tests pass (2/2)
✅ Test infrastructure ready for Phase 2 TDD development

## Context

This is prerequisite work for Phase 2 User Story 1 (Invite Requests). Following constitution's Test-Driven Development requirement, tests must be written before implementation - but test infrastructure must exist first.
